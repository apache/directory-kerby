package org.haox.kerb.crypto.key;

import org.haox.kerb.crypto.enc.EncryptProvider;

public class Aes256KeyMaker extends AesKeyMaker {

    public Aes256KeyMaker(EncryptProvider encProvider) {
        super(encProvider);
    }
}
