package org.haox.kerb.crypto.key;

import org.haox.kerb.crypto.enc.EncryptProvider;

public class Aes128KeyMaker extends AesKeyMaker {

    public Aes128KeyMaker(EncryptProvider encProvider) {
        super(encProvider);
    }
}
